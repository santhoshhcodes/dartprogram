void main() {
//   Count 
  String name = "kannan";
  Map<String,int> count ={};
  for(int i =0 ; i< name.length ; i++){
    String num = name[i];
    count[num] = (count[num] ?? 0) + 1;    
  }
  print("count of Characters in $name : $count");
}